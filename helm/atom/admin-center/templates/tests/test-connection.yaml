apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "admin-center.fullname" . }}-test-connection"
  labels:
    {{- include "admin-center.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "admin-center.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
