apiVersion: v1
kind: Service
metadata:
  name: {{ include "oauth-center.fullname" . }}
  labels:
    {{- include "oauth-center.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oauth-center.selectorLabels" . | nindent 4 }}
