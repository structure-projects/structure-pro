apiVersion: v1
kind: Service
metadata:
  name: {{ include "content-center.fullname" . }}
  labels:
    {{- include "content-center.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "content-center.selectorLabels" . | nindent 4 }}
