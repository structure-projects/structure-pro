apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rabbitmq3.fullname" . }}-test-connection"
  labels:
    {{- include "rabbitmq3.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rabbitmq3.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
