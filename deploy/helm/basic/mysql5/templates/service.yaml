apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql5.fullname" . }}
  labels:
    {{- include "mysql5.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: mysql
      protocol: TCP
      name: mysql
  selector:
    {{- include "mysql5.selectorLabels" . | nindent 4 }}
