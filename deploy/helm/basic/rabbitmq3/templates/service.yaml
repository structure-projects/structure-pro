apiVersion: v1
kind: Service
metadata:
  name: {{ include "rabbitmq3.fullname" . }}
  labels:
    {{- include "rabbitmq3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .name }}
      protocol: TCP
    {{- end }}
  selector:
    {{- include "rabbitmq3.selectorLabels" . | nindent 4 }}
