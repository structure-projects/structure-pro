apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rocketmq.fullname" . }}-test-connection"
  labels:
    {{- include "rocketmq.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rocketmq.fullname" . }}-namesrv:{{ .Values.services.namesrv.port }}']
  restartPolicy: Never
