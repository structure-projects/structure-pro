apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "job-center.fullname" . }}-test-connection"
  labels:
    {{- include "job-center.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "job-center.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
