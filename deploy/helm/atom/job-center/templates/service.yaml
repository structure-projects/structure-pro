apiVersion: v1
kind: Service
metadata:
  name: {{ include "job-center.fullname" . }}
  labels:
    {{- include "job-center.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "job-center.selectorLabels" . | nindent 4 }}
