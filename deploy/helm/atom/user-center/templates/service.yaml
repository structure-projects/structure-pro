apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-center.fullname" . }}
  labels:
    {{- include "user-center.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "user-center.selectorLabels" . | nindent 4 }}
