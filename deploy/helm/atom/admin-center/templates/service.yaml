apiVersion: v1
kind: Service
metadata:
  name: {{ include "admin-center.fullname" . }}
  labels:
    {{- include "admin-center.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "admin-center.selectorLabels" . | nindent 4 }}
