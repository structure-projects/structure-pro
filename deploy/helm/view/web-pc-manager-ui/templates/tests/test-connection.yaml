apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "web-pc-manager-ui.fullname" . }}-test-connection"
  labels:
    {{- include "web-pc-manager-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "web-pc-manager-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
