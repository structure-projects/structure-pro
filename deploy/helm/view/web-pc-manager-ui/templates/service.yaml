apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-pc-manager-ui.fullname" . }}
  labels:
    {{- include "web-pc-manager-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-pc-manager-ui.selectorLabels" . | nindent 4 }}
