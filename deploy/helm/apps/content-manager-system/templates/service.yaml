apiVersion: v1
kind: Service
metadata:
  name: {{ include "content-manager-system.fullname" . }}
  labels:
    {{- include "content-manager-system.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "content-manager-system.selectorLabels" . | nindent 4 }}
