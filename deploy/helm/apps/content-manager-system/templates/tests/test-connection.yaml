apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "content-manager-system.fullname" . }}-test-connection"
  labels:
    {{- include "content-manager-system.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "content-manager-system.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
