apiVersion: v1
kind: Service
metadata:
  name: {{ include "manager-system.fullname" . }}
  labels:
    {{- include "manager-system.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "manager-system.selectorLabels" . | nindent 4 }}
