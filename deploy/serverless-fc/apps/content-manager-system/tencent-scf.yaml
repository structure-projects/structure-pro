appId: {appId}
component: scf
name: content-manager-system
stage: prod
org: {org}
app: content-manager-system-app
inputs:
  name: content-manager-system
  src:
    src: ./
    exclude:
      - .env
  region: ap-guangzhou
  runtime: CustomContainer
  description: Content Manager System Service
  namespace: default
  type: web
  memorySize: 2048
  timeout: 300
  initTimeout: 300
  cls:
    logsetId: ''
    topicId: ''
  layers: []
  environment:
    variables:
      JAVA_OPTS: -Xms256m -Xmx1024m
      PARAMS: -Dfile.encoding=UTF-8 -Dspring.profiles.active=pro -Djava.security.egd=file:/dev/./urandom -Duser.timezone=Asia/Shanghai

  containerConf:
    image: registry.cn-hangzhou.aliyuncs.com/structured/structured-content-manager-system:latest
    containerPort: 8080
    imageTag: latest
  vpcConfig:
    vpcId: ''
    subnetId: ''
  triggers:
    - type: apigw
      parameters:
        name: content-manager-system-apigw
        protocols:
          - http
          - https
        serviceName: serverless
        description: API Gateway trigger for content-manager-system
        environment: release
        endPoints:
          - path: /
            method: ANY
